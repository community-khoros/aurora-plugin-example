#import "./TopUser.fragment.graphql"

query TopUsers {
  users(
    sorts: {
      kudosReceivedCount: { direction: DESC, order: 2 }
      messagesCount: { direction: DESC, order: 3 }
    }
    first: 5
  ) {
    edges {
      node {
        ...TopUser
      }
    }
  }
}
